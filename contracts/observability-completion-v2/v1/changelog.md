# observability-completion-v2 v1

## 1.0.1

- Added `type_index` reason taxonomy to contract (`allowed_type_index_reasons` + prefix).
- Added dedicated precompute runtime projection keys (queue/exec/build, counters + histograms).

## 1.0.0

- Initial baseline for completion v2 observability labels and metric prefixes.
- Migration note: initial release, no migration required.
