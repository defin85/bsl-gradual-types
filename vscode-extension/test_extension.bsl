// Тестовый файл BSL для проверки функциональности расширения
// Test BSL file for extension functionality verification

Функция ТестоваяФункция(Параметр1, Параметр2 = Неопределено) Экспорт
    // Тестирование поиска типов
    МойМассив = Новый Массив;
    МойМассив.Добавить("Тестовая строка");
    
    // Тестирование поиска методов
    Размер = МойМассив.Количество();
    
    // Тестирование совместимости типов  
    МояСтрока = СокрЛП(Параметр1);
    
    // Тестирование вызовов методов
    ТаблицаЗначений = Новый ТаблицаЗначений;
    ТаблицаЗначений.Колонки.Добавить("Наименование", Новый ОписаниеТипов("Строка", , Новый КвалификаторыСтроки(50)));
    
    Возврат МойМассив;
КонецФункции

Процедура ТестоваяПроцедура() Экспорт
    // Тестирование глобальных функций
    Сообщить("Тест выполнен успешно");
    
    // Тестирование условий
    Если ТипЗнч(Неопределено) = Тип("Неопределено") Тогда
        Сообщить("Тип определен корректно");
    КонецЕсли;
    
    // Тестирование циклов
    Для Индекс = 1 По 10 Цикл
        // Код цикла
    КонецЦикла;
    
    // Тестирование исключений
    Попытка
        Результат = 10 / 0;
    Исключение
        Сообщить("Обработано исключение: " + ОписаниеОшибки());
    КонецПопытки;
КонецПроцедуры