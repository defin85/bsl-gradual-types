## 1. Proposal / Design
- [ ] 1.1 Инвентаризировать все текущие определения CFG и точки их использования (IR CFG vs Domain CFG) и зафиксировать в `design.md`.
- [ ] 1.2 Выбрать «канонический» CFG формат и стратегию миграции (с чёткими критериями успеха и шагами).

## 2. Implementation
- [ ] 2.1 Устранить конфликт имён CFG (переименование/удаление одного из типов или явное разведение имён).
- [ ] 2.2 Реализовать построение CFG в `bsl-analysis-v2` при AST → IR (заменить `None` на реальный CFG).
- [ ] 2.3 Обновить потребителей flow-sensitive анализа (narrowing/null-safety) так, чтобы они использовали канонический CFG и данные были доступны из v2 snapshot.
- [ ] 2.4 Принять решение по экспериментальным анализаторам в `bsl-runtime` (интегрировать/переместить/удалить) и выполнить выбранный вариант.
- [ ] 2.5 Обновить/добавить тесты, фиксирующие:
  - наличие CFG в v2 pipeline на representative примерах,
  - отсутствие дублирующих CFG типов/имен,
  - базовые flow-sensitive сценарии (narrowing/null-safety) в рамках v2.

## 3. Docs / Tooling
- [ ] 3.1 Расширить «защиту от дрейфа» документации: определить список документов, где ссылки на пути являются «инструкцией к действию» (включая не только `docs/guides/*`, но и `backend/src/README.md` и т.п.).
- [ ] 3.2 Добавить автоматическую проверку, что такие пути существуют (локальный скрипт и/или CI гейт, в зависимости от политики репозитория).

## 4. Validation
- [ ] 4.1 `openspec validate refactor-flow-sensitive-cfg --strict --no-interactive`.
- [ ] 4.2 `cargo test --workspace` (после реализации; до архивации change).

