Функция Тест()
    // Milestone 3.5: Проверка Flow-Sensitive Analysis
    ТаблицаЗначенійТип = Новый ТаблицаЗначений;
    Кол = ТаблицаЗначенійТип.Количество();

    // Milestone 3.7: Проверка Semantic Diagnostics
    МассивДанных = Новый Массив;
    МассивДанных.НесуществующийМетод();

    Число = 42;
    Число.Добавить(1);
КонецФункции
