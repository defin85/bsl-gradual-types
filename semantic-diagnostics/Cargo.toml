[package]
name = "bsl-semantic-diagnostics"
version.workspace = true
edition.workspace = true
authors.workspace = true
license.workspace = true
description = "Semantic diagnostics (validation visitor) extracted from backend"

[lib]
name = "bsl_semantic_diagnostics"
path = "src/lib.rs"

[dependencies]
bsl-shared = { workspace = true }
tracing = { workspace = true }

