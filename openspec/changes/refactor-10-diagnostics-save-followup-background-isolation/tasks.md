## 1. Implementation
- [x] 1.1 Introduce an explicit non-interactive `did_save_followup` execution/admission lane in the facade/runtime contract, carry that lane identity separately from both `SemanticOperation::Diagnostics` and the binary `CpuWorkClass` taxonomy, expose one stable canonical lane/raw-label value for telemetry, and define one operator-visible quota as end-to-end follow-up slots rather than split writer-vs-CPU quotas.
- [x] 1.2 Implement an explicit outer admission boundary for `did_save_followup` before existing scarce writer FIFO and blocking CPU permit acquisition, with a diagnostics-runtime-owned latest-wins arbiter/queue above applied/shadow/ready/fallback branch fan-out that re-checks supersession and effective quota before scarce resource acquisition; a nominal tag on the current generic `Background` queue/permit paths is not sufficient.
- [x] 1.3 Hold one end-to-end follow-up slot guard from outer admission through writer/runtime preparation, blocking CPU execution, and the final pre-publish supersession/quota/disposition decision, releasing the scarce slot before outbound publish/output wait so publish-path contention stays separately observable, rather than introducing separate writer-vs-CPU quotas or raw CPU-only semaphore semantics, and propagate this guard/token opaquely into downstream facade/runtime helpers instead of creating branch-local admission owners.
- [x] 1.4 Route writer-owned applied-state, same-version shadow-state, ready-artifacts, and didSave fallback prepare/query stages through the same lane, and remove the current applied-state bypass around lane-aware prepare/admission hooks while preserving supersession and latest-wins semantics via explicit stale-shedding checkpoints before scarce resources are consumed and again before publish.
- [x] 1.5 Implement lane-aware arbitration in the existing scheduler/runtime queue without borrowing interactive capacity, without introducing a third `CpuWorkClass`, without forking a separate writer scheduler, and without increasing total bounded runtime/CPU parallelism beyond the pre-change budget; quota semantics MUST stay global process-wide while queued work uses latest-only per-file retention plus fair rotation across distinct files.
- [x] 1.6 Add stable runtime-config knob(s) for didSave-follow-up lane quota/permits, set the default effective quota to `1`, define `quota=0` as explicit disable of new `didSave + idle_heavy` admissions, re-check the effective quota for queued-but-not-started work at scarce-lane admission time, apply runtime quota changes at the outer lane boundary for future admissions only, and expose the effective value through runtime-config snapshot.
- [x] 1.7 Export dedicated lane telemetry immediately in explicit additive schema: queue-wait/exec samples and saturation gauges MUST expose a first-class bounded canonical `lane` surface, or a semantically equivalent dedicated runtime-lane family, with stable value `did_save_followup`; legacy `interactive/background` / `work_class` projections remain compatibility views only, registry/projection tests are updated explicitly, and the new additive lane/outcome taxonomy remains a valid input surface for future `rewrite-v2-observability-perf-pipeline` work instead of being deferred behind it.
- [x] 1.8 Extend diagnostics save/outcome contracts so `disabled_by_config` is represented canonically as a dedicated non-cancellation terminal disposition/outcome across terminal disposition reporting, save timeline, incident-bundle summary projection, and metrics/counters via the shared outcome/disposition surface rather than only as an ad-hoc trace string.

## 2. Validation
- [ ] 2.1 Add deterministic saturation regressions where unrelated background work no longer dominates post-fastlane didSave follow-up as the default gate.
- [x] 2.2 Add applied-state regressions where writer-owned applied-state follow-up stays on the isolated lane instead of inheriting generic background runtime queue backlog by default.
- [x] 2.3 Add forced-fallback regressions where didSave follow-up lane/supersession metadata is evaluated at the outer admission boundary before generic background writer/runtime scarcity and the follow-up no longer inherits generic background writer/runtime backlog by default.
- [x] 2.4 Add save-storm regressions proving latest-wins behavior under repeated same-file `didSave`, including stale queued follow-up shedding before it becomes the default blocker for the newer cycle.
- [x] 2.5 Add runtime-config regressions proving the default effective quota is `1` and yields bounded single-slot behavior without net-new save-storm parallel fan-out before any operator override is applied.
- [x] 2.6 Add multi-file fairness regressions proving global quota plus latest-only per-file retention does not turn one noisy file into a raw FIFO blocker for another file waiting on the dedicated lane.
- [x] 2.7 Add runtime-config regressions proving `quota=0` disables new `didSave + idle_heavy` follow-up admissions without affecting `save_fastlane`, that queued-but-not-started follow-up re-checks the effective quota at admission time before consuming scarce capacity, and that already admitted work is not retroactively reclassified mid-flight when the quota changes later.
- [ ] 2.8 Add arbitration regressions proving the dedicated lane is carved from the existing bounded budget, cannot borrow interactive reserved capacity, does not increase total runtime/CPU permit count, and has exactly one outer arbitration owner above branch fan-out rather than duplicate branch-local queues.
- [x] 2.9 Add telemetry contract regressions proving the dedicated lane is exported separately in additive metrics/trace schema via canonical lane identity, or semantically equivalent dedicated runtime-lane family, does not fall through legacy `work_class` normalization as the only visible representation, and that legacy `interactive/background` / `CpuWorkClass` projections remain binary compatibility views rather than the only representation.
- [x] 2.10 Add outcome-contract regressions proving `disabled_by_config` is emitted canonically in save trace, incident-bundle summary projection, and diagnostics pipeline terminal outcome/disposition reporting through the shared enum/counter surface as a dedicated non-cancellation value and is not counted as a cancellation outcome.
- [x] 2.11 Re-run existing save-fastlane/follow-up regressions to confirm no first-publish or correctness regression.
- [ ] 2.12 Capture representative `conf_big` live evidence showing bounded first publish and reduced follow-up queue wait under comparable mixed load.
- [x] 2.13 Run `openspec validate refactor-10-diagnostics-save-followup-background-isolation --strict --no-interactive`.
