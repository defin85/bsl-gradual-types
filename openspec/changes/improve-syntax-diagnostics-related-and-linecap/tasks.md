## 1. Proposal / Specs
- [ ] 1.1 Добавить delta-спеку для `bsl-intellisense-v2`: строгий line-cap для итоговых синтаксических diagnostics, related для незакрытых блоков, обобщение правила `Для` (ожидается `Цикл`).
- [ ] 1.2 `openspec validate improve-syntax-diagnostics-related-and-linecap --strict --no-interactive`.

## 2. Implementation (syntax)
- [ ] 2.1 Вынести единый финальный шаг нормализации синтаксических diagnostics (merge → rewrite/enrich → line-cap → sort).
- [ ] 2.2 Применить line-cap ко всем синтаксическим diagnostics в `ParseResult.syntax_errors` (включая semicolon/`Новый` эвристики) с явной политикой приоритетов.
- [ ] 2.3 Реализовать masking строки (комментарии `//`, строковые литералы) для безопасного поиска ключевых слов без смещения индексов.
- [ ] 2.4 Обобщить правило `Для`: span на первый неожиданный токен между `По` и `Цикл`, сообщение “ожидается `Цикл`” даже при мусорном токене.
- [ ] 2.5 Enrich related для незакрытой `Попытка` при распознавании по `ParseError` (добавлять “Начало блока: Попытка” если related отсутствует).

## 3. Tests
- [ ] 3.1 Юнит‑тест: конкурирующие синтаксические diagnostics на одной строке → остаётся 1 по приоритету (включая семиколон/`Новый`).
- [ ] 3.2 Юнит‑тест: `Для ... По 0 abc Цикл` (мусорный токен) → сообщение “ожидается `Цикл`”, span на `abc`.
- [ ] 3.3 Юнит‑тест: ключевое слово `Шаг` внутри строки/комментария не должно триггерить rewrite.
- [ ] 3.4 Юнит‑тест: незакрытая `Попытка` распознана по `ParseError` → diagnostics содержит related “Начало блока: Попытка”.

## 4. Quality gates
- [ ] 4.1 `cargo test -p bsl-syntax` (или соответствующий пакет для `syntax/`).
- [ ] 4.2 (опционально) smoke: `cargo test -p bsl-analysis-v2`.

