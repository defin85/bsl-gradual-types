{
  "mcpServers": {
    "bsl-agent": {
      "command": "cargo",
      "args": [
        "run",
        "-p",
        "bsl-agent",
        "--"
      ],
      "env": {
        "RUST_LOG": "bsl_agent=info",
        "BSL_AGENT_HTTP_ADDR": "127.0.0.1:0"
      }
    }
  }
}
