# Finish-to-100 Closure - 2026-04-27

## Closed Gaps

- Terminal didSave exact-producer lifecycle states now release the bounded
  save-family lowering reuse seed through the default LSP document-sync path.
- Seed eviction reasons now distinguish terminal cleanup, supersession,
  cancellation, failure, continuity loss, cache disablement, and bounded
  retention.
- Unsafe lowering-reuse negative tests now assert the explicit `not_reusable`
  plan failure reason rather than only checking for `None`.
- VS Code incident-bundle coverage now proves `reuse_seed_eviction_reason` and
  `reuse_plan_failure_reason` survive the report object, raw diagnostics-save
  JSON, and human-readable summary.

## Requirement -> Code -> Test

- Explicit bounded save-family continuity:
  `bsl-runtime/src/system/parser_coordinator.rs`
  (`LoweringReuseSaveFamilySeedStore`, `release_lowering_reuse_save_family_seed`)
  -> `exact_lowering_reuse_plan_uses_save_family_seed_after_ast_cache_clear`.
- Terminal/superseded/cancelled/failed cleanup:
  `backend/src/bin/lsp_server/server/language_server/impl_document_sync.rs`
  (`record_did_save_exact_producer_lifecycle_*_v2`,
  `lowering_reuse_seed_eviction_reason_for_did_save_lifecycle_state_v2`)
  -> `exact_lowering_reuse_seed_release_records_terminal_cleanup_eviction_reason`
  and `terminal_didsave_lifecycle_states_map_to_lowering_reuse_seed_evictions`.
- Fail-visible required rebuild:
  `bsl-runtime/src/system/parser_coordinator.rs`
  (`ParseSnapshotProgramLoweringReusePlanFailureReason`)
  -> `exact_lowering_reuse_plan_fails_closed_for_local_var_declaration_edit`,
  `exact_lowering_reuse_plan_fails_closed_for_edit_inside_try_region`, and
  `exact_lowering_reuse_seed_capacity_eviction_is_reported_as_failure_reason`.
- Projection preservation:
  `vscode-extension/src/test/suite/observabilityIncidentBundle.test.ts`
  -> `program-lowering seed eviction evidence should survive report, raw JSON, and summary`.

## Verification

- `cargo fmt --all`
- `cargo fmt --all -- --check`
- `cargo test -p bsl-runtime exact_lowering_reuse -- --nocapture`
- `cargo test -p bsl-backend terminal_didsave_lifecycle_states_map_to_lowering_reuse_seed_evictions -- --nocapture`
- `cargo check --workspace --all-targets`
- `cargo clippy --workspace --all-targets -- -D warnings`
- `npm --prefix vscode-extension run compile:fast`
- `BSL_TEST_GREP='program-lowering seed eviction evidence should survive report, raw JSON, and summary|program-lowering tail should keep missing reuse evidence fail-visible' node scripts/run-vscode-extension-tests.js`
- `npm --prefix vscode-extension run lint`
- `openspec validate refactor-60-didsave-lowering-reuse-continuity --strict --no-interactive`

## Notes

- Beads has no matching work items for this spec id: `bd list --all --spec
  refactor-60-didsave-lowering-reuse-continuity --json` returned `[]`.
- Representative live bundle recapture was not rerun during this finish pass;
  this closure targets the mandatory code/test gaps found by implementation
  review.
