# Validation Status

Date: 2026-04-23

## Targeted Passes

- `cargo test -p bsl-backend --bin bsl-lsp-server p7_did_save_discards_stale_completed_previous_version_type_index_task -- --nocapture`
- `cargo test -p bsl-backend --bin bsl-lsp-server p7_did_save_same_version_rebuild_reuses_current_revision_parse_result_seed -- --nocapture`
- `cargo test -p bsl-backend --bin bsl-lsp-server p7_did_save_followup_prefers_inflight_same_version_ready_snapshot_before_shadow_state -- --nocapture`
- `cargo test -p bsl-backend --bin bsl-lsp-server p56_real_conf_big_diagnostics_representative_save_followup_bundle_live -- --nocapture`
- `openspec validate refactor-49-save-followup-same-version-ready-snapshot-rebuild-bounding --strict --no-interactive`

## Closed Runtime Seams

- `didChange` and `didSave` now discard stale completed previous-version type-index tasks before
  same-version follow-up observation, so waiting-phase representative traces no longer inherit a
  fake `type_index_task_state_after_timeout`.
- Same-version `DidSaveFollowup` rebuilds with non-empty `parser_edits` can now prime the parser
  AST cache from the previous ready snapshot when that snapshot is syntax-complete,
  parse-error-free, older than the requested version, and text-distinct from the current save
  target.
- The new targeted regression keeps the stale previous-version type-index cleanup fail-closed, and
  the preserved same-version rebuild regressions stay green on the same tree.

## Representative Live Evidence

- The current representative report lives at
  `backend/tests/perf/reports/refactor-49-save-followup-same-version-ready-snapshot-rebuild-bounding-real-conf-big-diagnostics-representative-save-followup-bundle-live.json`
  and was refreshed on `2026-04-23 12:59:20 +0300`.
- The accepted detached cycle now shows query-dominated follow-up instead of rebuild-dominated
  exact assembly:
  `followup_semantic_path=detached_ready_artifacts`,
  `followup_ready_snapshot_parse_exec_ms=150`,
  `followup_ready_snapshot_program_lowering_ms=137`,
  `followup_publish_elapsed_ms=1363`,
  `followup_publish_semantic_diagnostics_query_ms=1118`,
  `semantic_query_dominates_parse_exec=true`,
  `program_lowering_reuse_outcome=routine_body_reuse`,
  `program_lowering_reuse_plan_build_source=owned`,
  `program_lowering_reuse_plan_take_if_unique_hit=true`,
  `program_lowering_reused_lowering_units=2079`, and
  `program_lowering_rebuilt_lowering_units=9`.
- The report summary now closes the original residual directly:
  `followup_ready_snapshot_rebuild_dominated_shadow_state_count=0`,
  `representative_canonical_residual_mix=waiting_phase_queue_blocker_not_parse_exec_rebuild`, and
  `max_followup_ready_snapshot_parse_exec_vs_baseline_ceiling_delta_ms=-3177`.
- The same representative run still contains waiting-phase `shadow_state` cycles, but they are now
  explicitly attributed to `followup_ready_snapshot_timeout_phase=waiting` /
  `followup_ready_snapshot_timeout_leaf=waiting` and the test accepts them only because the saved
  exact ready snapshot later materializes on the same save target.

## Verdict

- `refactor-49` is now acceptance-ready on its intended seam: same-version `didSave` heavy
  follow-up no longer fails the representative `conf_big` gate because of a cold
  `parse_exec -> exact_ready_snapshot_assembly -> program_lowering` rebuild residual.
- Remaining waiting-phase queue pressure is still visible in the report, but it is no longer a
  rebuild-path blocker and therefore no longer keeps this change open.
