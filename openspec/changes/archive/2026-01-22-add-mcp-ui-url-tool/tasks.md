## 1. Контракт и UX
- [x] 1.1 Зафиксировать имя tool `ui_url`, формат ответа и поведение при выключенном UI.
- [x] 1.2 Зафиксировать, что tool read-only и не влияет на запуск/порт UI.

## 2. Реализация: MCP tool
- [x] 2.1 Добавить `ui_url` в список MCP tools `bsl-agent`.
- [x] 2.2 Реализовать чтение URL текущего инстанса (без shell), используя runtime registry/текущее состояние процесса.
- [x] 2.3 Обновить интеграционный smoke-тест списка tools (stdio), чтобы включать `ui_url`.

## 3. Тестирование
- [x] 3.1 Интеграционный тест: при включённом HTTP UI `ui_url` возвращает `http://localhost:<port>`.
- [x] 3.2 Интеграционный тест: при выключенном HTTP UI `ui_url` возвращает `enabled=false` (или эквивалентный сигнал) без ошибки.

## 4. Документация
- [x] 4.1 Обновить `bsl-agent/README.md`: как получить URL через MCP tool `ui_url` (и чем это отличается от CLI).
