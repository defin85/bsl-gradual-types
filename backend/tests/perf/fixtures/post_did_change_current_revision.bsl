Процедура ЛокальныйМетод() Экспорт
КонецПроцедуры

Процедура Тест()
    Массив = Новый Массив;
    ДляHover = Массив.Количество();
    ДляDefinition = ЛокальныйМетод();
    ДляType = Массив;
    ДляMembers = Массив.
    ДляCompletion = Массив.
КонецПроцедуры
