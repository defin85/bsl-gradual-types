# Superseded

Этот change superseded и заменен единым change:
- `update-unified-form-and-applied-module-semantics`

Причина: правила для `FormModule` и applied module contexts описывают единый pipeline и должны поддерживаться одним источником требований.
