//! Backend facade for AST -> IR conversion.
//!
//! The implementation lives in `bsl-semantic` to allow sharing with `bsl-analysis-v2`
//! without depending on `bsl-backend`.

pub use bsl_semantic::*;
