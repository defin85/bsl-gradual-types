// Тестовый модуль BSL для демонстрации типов

Функция СоздатьМассив(Размер)
    Результат = Новый Массив(Размер);
    Возврат Результат;
КонецФункции

Функция ОбработатьТаблицу(ТаблицаЗначений)
    Для Каждого Строка Из ТаблицаЗначений Цикл
        Значение = Строка.Количество;
        Если Значение > 0 Тогда
            Сообщить("Найдено: " + Строка(Значение));
        КонецЕсли;
    КонецЦикла;
КонецФункции

Процедура ИнициализацияПеременных()
    СписокСтрок = Новый СписокЗначений;
    СписокСтрок.Добавить("Первая строка");
    СписокСтрок.Добавить("Вторая строка");
    
    Структура = Новый Структура("Ключ1, Ключ2", "Значение1", 42);
    Соответствие = Новый Соответствие;
    Соответствие.Вставить("тест", Истина);
КонецПроцедуры