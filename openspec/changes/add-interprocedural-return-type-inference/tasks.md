## 1. Подготовка данных и кэшей (config modules)
- [ ] 1.1 Расширить модель `ParsedModuleData` так, чтобы хватало данных для interprocedural return inference (в т.ч. отметка динамики/неопределённости).
- [ ] 1.2 Обновить (и при необходимости версионировать) ключи/формат disk cache `config-module-parse`, чтобы корректно инвалидировать старые записи.

## 2. Межпроцедурный вывод return-типа в индексации конфигурации
- [ ] 2.1 Построить граф вызовов (включая локальные вспомогательные функции внутри модуля) на основе `call_sites`.
- [ ] 2.2 Реализовать SCC + фикс‑пойнт вычисления return‑summaries для функций/процедур.
- [ ] 2.3 Преобразовать return‑summary в строковый `return_type` (union) для экспортных сигнатур и записывать в `SignatureIndex`.
- [ ] 2.4 Гарантировать консервативную семантику: сохранять известные типы, помечать weak/dynamic при неопределённости, не “затирать” известное до `Unknown`.

## 3. Overlay для открытых файлов (analysis-v2 / salsa)
- [ ] 3.1 Добавить вычисление return‑summaries для open files и общий overlay‑слой “поверх” `SignatureIndex`.
- [ ] 3.2 Обеспечить, что изменения в одном открытом файле влияют на типы вызовов в других открытых файлах (между всеми open files).
- [ ] 3.3 Обеспечить отсутствие блокирующего I/O в hot path IDE‑запросов; использовать только v2 snapshot/queries и уже загруженные зависимости.

## 4. Интеграция в резолв и UX‑сигналы
- [ ] 4.1 Использовать новые return‑типы в call-site типизации (completion/hover/type-at-position/signatureHelp) для пользовательских экспортов.
- [ ] 4.2 Отражать “weak/dynamic” через `Certainty::InferredWeak` и/или причины неопределённости (без потери известной части union).

## 5. Тестирование и регрессии
- [ ] 5.1 Юнит‑тесты для SCC/fix‑point и семантики union+dynamic.
- [ ] 5.2 Интеграционные тесты: межмодульный вызов экспортной функции общего модуля даёт тип на call site.
- [ ] 5.3 Интеграционные тесты: overlay для двух открытых файлов обновляет типы без переиндексации конфигурации.

## 6. Документация
- [ ] 6.1 Обновить архитектурную заметку/roadmap (кратко): где живут return‑summaries, как работают кэши и overlay.

