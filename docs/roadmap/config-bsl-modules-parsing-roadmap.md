# Roadmap: Парсинг BSL-модулей конфигурации (методы, фасеты, валидация)

**Статус:** 🟢 СДЕЛАНО (C1–C7 ✅)  
**Приоритет:** HIGH  
**Цель:** сделать так, чтобы код из выгруженной конфигурации (`*.bsl`) участвовал в **валидации** и давал **методы/сигнатуры** для `hover/completion/validation`, с корректной привязкой к **фасетам** (Manager/Object/…) и учётом видимости (**Экспорт** = публичный API, без **Экспорт** = приватный внутри модуля).

---

## Фактический прогресс (проверено по репозиторию)

Проверка выполнена по коду и тестам:
- Discovery модулей (`ObjectModule/ManagerModule/RecordSetModule`) вызывается для каждого объекта метаданных: `backend/src/data/loaders/config_metadata_parser/discovery.rs:440`.
- Индексация экспортных методов из модулей конфигурации есть: `backend/src/data/loaders/config_bsl_modules.rs:1`.
- Экспортные сигнатуры добавляются в `SignatureIndex` после `bsl.parseConfiguration`: `backend/src/bin/lsp_server/commands/configuration.rs:293`.
- Валидация контекста вызова учитывает `ContextRequirements` сигнатуры: `backend/src/application/semantic_validation_visitor/validators/call_validator.rs:1`.
- Возвраты (MVP) выводятся по `Возврат <expr>` и пишутся в сигнатуру: `backend/src/data/loaders/config_bsl_modules.rs:761`.
- Типы параметров (MVP) инферятся по call-site между модулями (CommonModule/Manager): `backend/src/data/loaders/config_bsl_modules.rs:599`.
- Интеграционный тест на реальной конфигурации (если доступна `examples/conf_big`) есть: `backend/tests/config_big_modules_indexing_test.rs:1`.
- `cargo test --workspace` проходит (проверено в workspace).

## Контекст (текущее состояние)

На момент написания:
- XML-метаданные конфигурации парсятся и загружаются в `TypeRepository` как `RawTypeData`, но `methods` для конфигурационных типов по-прежнему не заполняются на этом шаге (методы подтягиваются отдельной индексацией BSL-модулей): `backend/src/data/loaders/config_metadata_parser/converter.rs:94`.
- Discovery умеет находить пути к `ObjectModule.bsl / ManagerModule.bsl / RecordSetModule.bsl` для всех объектов метаданных; формы обнаруживаются через `discover_forms_for_object()` для всех типов (если `Forms/` отсутствует — быстрый early return): `backend/src/data/loaders/config_metadata_parser/discovery.rs:440`.
- LSP `Go To Definition`: для типов по-прежнему использует `module_paths` как навигацию по файлам, а для методов/функций использует индекс `method -> location`, построенный индексацией конфигурационных модулей (`*.bsl`): `backend/src/bin/lsp_server/handlers/definition.rs:28`.
- В pipeline загрузки конфигурации есть отдельный шаг индексации `*.bsl` модулей конфигурации (извлечение экспортов/контекстов/локаций): `backend/src/bin/lsp_server/commands/configuration.rs:293`.
- Единственный источник данных о методах платформы — `syntax_helper`, а конфигурационные сигнатуры добавляются в индекс отдельным шагом (не через `SignatureDataSource`): `backend/src/data/loaders/signature_sources.rs:1`, `backend/src/bin/lsp_server/commands/configuration.rs:293`.

---

## Нефункциональные требования

- **Right-Sized Architecture:** не плодить новые “сервисы на каждый чих”; добавлять один понятный загрузчик модулей конфигурации.
- **Semantic IR / честная проверка:** интеграция через существующий pipeline `AST → IR → Validation/Resolver`, с проверками через тесты и grep перед отчётами.
- **Без запуска серверов:** LSP/Web серверы не трогаем; изменения — в коде загрузки/анализа.

---

## Цели и не-цели

### Цели
- Экспортные процедуры/функции из модулей конфигурации становятся **доступными как методы** соответствующих конфигурационных типов/фасетов.
- Приватные (без `Экспорт`) **не попадают в общий индекс сигнатур** (чтобы их не “видели” другие модули); локальная навигация/поиск определений остаётся задачей анализа конкретного файла.
- Методы из:
  - `CommonModules/*/Ext/Module.bsl`
  - `*/Ext/ObjectModule.bsl`
  - `*/Ext/ManagerModule.bsl`
  - `*/Ext/RecordSetModule.bsl`
  - (по мере необходимости) другие модульные типы
  — участвуют в индексации.
- Параметры/возвраты получают типы как минимум частично (см. ниже), с прозрачной `certainty`.

### Не-цели (на первом этапе)
- Полная семантическая модель платформы 1С (все директивы, все тонкости модульной области видимости, все особенности препроцессора).
- Полный межмодульный call-graph для всего проекта (можно идти инкрементально).

---

## Архитектура решения (высокоуровнево)

Добавляется отдельный слой: **Configuration BSL Module Loader**.

**Вход:**
- метаданные конфигурации (XML) + `module_paths` (если есть),
- свойства общих модулей (контексты выполнения из XML),
- доступ к файловой системе конфигурации.

**Выход:**
- “публичные” сигнатуры конфигурации (только `Экспорт`) в индекс сигнатур,
- локальная информация о приватных методах (для валидации внутри модуля),
- `method -> code location` для навигации (Go To Definition).

Принципиально: парсинг `.bsl` использует `tree-sitter-bsl` + `TreeSitterAdapter` (AST), чтобы не плодить второй парсер/грамматику.

---

## Привязка методов к фасетам и владельцам

### Модули объектов/менеджеров/наборов записей
- `ObjectModule.bsl` → фасет `Object` для типа-владельца (например `Справочники.Контрагенты` с `active_facet=Object`).
- `ManagerModule.bsl` → фасет `Manager`.
- `RecordSetModule.bsl` → пока маппится на фасет `Object` (можно выделить отдельный фасет позже, если понадобится).

### Общие модули
- Экспортные методы доступны как:
  - методы типа `CommonModule` (singleton) **и/или**
  - глобальные функции, если `Global=true` (нужна явная политика в проекте: алиас в глобальном namespace).

---

## Видимость

- **Экспорт** → публичные сигнатуры (видимы из других модулей).
- **Без Экспорт** → приватные (видимы только внутри конкретного `*.bsl` файла модуля).

Технически: приватные не должны попадать в общий `SignatureIndex`, иначе другие модули начнут “видеть” их как существующие.

---

## Типизация параметров и возвращаемых значений

### Уже сделано (MVP)
- **Возвраты:** собираются `Возврат <expr>`, итоговый `return_type` = union типов `<expr>`; если `Возврат` отсутствует → `Неопределено`.
- **Параметры:** call-site inference для части вызовов экспортных методов (см. C6).

### План (следующие шаги)
- **Intra-procedural constraints (внутри функции):** если параметр используется как объект с методом/свойством — ограничивать тип; учитывать проверки `Если p = Неопределено` / `ТипЗнч` / `ЭтоТип` как narrowing.
- **Итеративное уточнение по SCC графа вызовов (опционально):** фикс-поинт по компонентам сильной связности, чтобы распространять типы между процедурами.

Важно: все выводы должны сохранять `certainty` и не ломать flow-sensitive модель внутри файла.

---

## Milestones (вертикальные срезы)

### C1: Расширить discovery модулей на все типы метаданных ✅ (сделано)
**Цель:** для всех релевантных объектов метаданных заполнять `module_paths` (не только `Document/Catalog`).

**Критерии успеха:**
- discovery находит `ObjectModule/ManagerModule/RecordSetModule` для регистров и других объектов, где эти модули есть.
- тесты на структуру путей и обнаружение.

**Проверка:**
- ✅ discovery вызывается при парсинге каждого объекта: `backend/src/data/loaders/config_metadata_parser/discovery.rs:440`.
- ✅ тесты discovery/CodeLocation: `backend/tests/config_object_modules_test.rs:17`.

### C1F: Парсинг форм не только для Document/Catalog ✅ (сделано)
**Цель:** расширить discovery/парсинг форм на другие виды метаданных (например, отчёты/обработки/… по реальной потребности), чтобы контексты и модули форм корректно учитывались не только у документов/справочников.

**Критерии успеха:**
- `discover_forms_for_object()` используется как основной API обнаружения форм (с корректным `owner_type`) для всех объектов метаданных.
- `metadata.forms` заполняется для новых типов, где формы присутствуют.
- тесты на обнаружение форм и базовую загрузку/парсинг `Form.xml`.

**Проверка:**
- ✅ формы пробуются для каждого объекта (при наличии `Forms/`): `backend/src/data/loaders/config_metadata_parser/discovery.rs:447`.
- ✅ тесты на `discover_forms_for_object()`: `backend/tests/config_forms_parsing_test.rs:143`.

### C2: Парсер BSL-модулей: извлечение процедур/функций + Export ✅ (MVP сделан)
**Цель:** получить список объявлений методов из `.bsl` модулей.

**Критерии успеха:**
- по `*.bsl` файлами модулей строится список `(name, params, is_export, directive/context, ...)`.
- корректно отсекаются приватные методы (не попадают в публичный индекс).

Примечание: MVP уже извлекает `name/params/is_export`, контекст из директив/модуля и `location` (используется для Go To Definition к объявлению метода/функции).

**Проверка:**
- ✅ парсинг модулей и сбор decls/call-sites: `backend/src/data/loaders/config_bsl_modules.rs:300`.
- ✅ unit-тесты загрузчика модулей: `backend/src/data/loaders/config_bsl_modules.rs:844`.

### C3: Интеграция публичных методов в `SignatureIndex` как “Configuration” ✅ (сделано)
**Цель:** экспортные методы становятся доступны как методы типов конфигурации в hover/validation.

**Критерии успеха:**
- `hover` по `Справочники.<X>` показывает методы из `ManagerModule` только в фасете Manager, из `ObjectModule` — только в Object.
- вызов несуществующего метода даёт диагностическую ошибку, а существующего экспортного — нет.

**Проверка:**
- ✅ индексация запускается после `parseConfiguration`: `backend/src/bin/lsp_server/commands/configuration.rs:293`.
- ✅ методы добавляются в репозиторий (SignatureIndex): `shared/src/domain/repository.rs:147`.

### C4: Контексты выполнения для конфигурационных методов ✅ (сделано)
**Цель:** сохранить/использовать `ContextRequirements` при валидации.

**Критерии успеха:**
- методы из CommonModule учитывают контексты из XML + директивы на методе (если есть).
- серверные вызовы в клиентском контексте корректно диагностируются (минимально — для форм).

**Фактический прогресс:**
- Для CommonModule контекст берётся из XML-свойств модуля; для Object/Manager/RecordSet модулей — `ServerOnly`; директива на методе переопределяет модульный контекст: `backend/src/data/loaders/config_bsl_modules.rs:244`.
- Валидатор проверяет доступность метода в текущем `RuntimeExecutionContext` по `ContextRequirements`: `backend/src/application/semantic_validation_visitor/validators/call_validator.rs:16`.

**Проверка:**
- ✅ интеграционный тест на предупреждение при вызове `ServerOnly` метода из `&НаКлиенте` (через `validate_semantics_with_file_path`, имитация form module path): `backend/tests/config_module_context_validation_test.rs:20`.

### C5: Типы возврата (MVP) ✅ (сделано)
**Цель:** базовый вывод return type по `Возврат`.

**Критерии успеха:**
- `SignatureIndex` для конфигурационных методов содержит return type хотя бы для простых кейсов (`Возврат 1`, `Возврат "x"`, `Возврат Новый Тип()` и т.п.).
- тесты: 2–3 функции с разными `Возврат` (один тип / разные ветки / нет возврата).

**Фактический прогресс:**
- Инференс return type реализован и покрыт unit-тестами: `backend/src/data/loaders/config_bsl_modules.rs:761`.

### C6: Типы параметров (MVP: call-site) ✅ (MVP)
**Цель:** вывести типы параметров экспортных методов по местам вызова.

**Критерии успеха:**
- для части экспортных методов параметры получают `type_name` по наблюдениям типов аргументов на вызовах (MVP: хотя бы литералы/Неопределено/Новый + простые локальные переменные).
- учитываются вызовы из других модулей в форме `ИмяОбщегоМодуля.Метод(...)` и `Справочники.<X>.<Метод>(...)` (минимальный межмодульный call-site).
- валидация аргументов начинает ловить очевидные ошибки по выведенным типам (с разумной `certainty`).

**Фактический прогресс:**
- Межмодульная инференция параметров реализована и покрыта unit-тестами: `backend/src/data/loaders/config_bsl_modules.rs:599`.

### C7: Полировка LSP UX (необязательное) ✅ (сделано)
**Цель:** улучшить диагностические сообщения и навигацию (например, go-to-definition к объявлению метода).

**Фактический прогресс:**
- Go To Definition поддерживает переход к объявлению экспортного метода в `*.bsl` (конфигурационные модули) по индексу `method -> location`.
- Для глобальных функций из global common modules хранится location (и его можно использовать для перехода).
- Есть fallback на локальные объявления функций/процедур в текущем файле (включая приватные), если repo не содержит location.
- Прогон проверки: `cargo test --workspace` проходит.

**Проверка:**
- ✅ тесты на go-to-definition: индексированный метод, индексированная глобальная функция, fallback на локальное объявление: `backend/tests/goto_definition_methods_test.rs:53`.

---

## Тестирование и проверка

- Unit-тесты на извлечение `Экспорт`, параметров, директив из `*.bsl`.
- Unit-тесты на return type и call-site inference (внутри `config_bsl_modules.rs`).
- Тесты на discovery/парсинг форм (в т.ч. не только `Document/Catalog`): `backend/tests/config_forms_parsing_test.rs:143`.
- Интеграционные тесты на “загрузка конфигурации → загрузка модулей → hover/validation”.
- Минимальный чек: `cargo test --workspace` для затронутых областей.
