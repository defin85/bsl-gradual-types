## 1. Проектирование и контракт
- [x] 1.1 Зафиксировать формат registry записи и расположение в state root (каталог, имя файла, поля).
- [x] 1.2 Зафиксировать поведение CLI при 0/1/>1 кандидате (коды выхода, сообщения, формат вывода).
- [x] 1.3 Зафиксировать селекторы инстанса: `--instance-id`, `--pid`, `--roots` (строгое совпадение roots).

## 2. Реализация: runtime registry (discovery)
- [x] 2.1 При успешном старте HTTP UI записывать registry запись (атомарно) с фактическим `ui_url`.
- [x] 2.2 Писать одну запись на инстанс (по `instance_id`), поддерживая параллельные запуски в одном `BSL_CACHE_DIR`.
- [x] 2.3 Обеспечить “живость” записей: в CLI фильтровать по healthcheck `GET /api/mcp/status` (с таймаутом).

## 3. Реализация: CLI сабкоманды `bsl-agent ui ...`
- [x] 3.1 Добавить `bsl-agent ui list` (табличный вывод) и `--all` для вывода “мёртвых” записей.
- [x] 3.2 Добавить `bsl-agent ui url` (plain URL) и селекторы `--instance-id/--pid/--roots`.
- [x] 3.3 При >1 совпадении без селектора завершаться ошибкой и печатать список кандидатов (instance_id/pid/ui_url).

## 4. Тестирование и валидация
- [x] 4.1 Интеграционный тест: при `BSL_AGENT_HTTP_ADDR=127.0.0.1:0` создаётся registry запись с корректным портом.
- [x] 4.2 Интеграционный тест: `bsl-agent ui url` возвращает URL при единственном инстансе и ошибку при нескольких.
- [x] 4.3 Интеграционный тест: `--roots` выбирает нужный инстанс при строгом совпадении roots.

## 5. Документация
- [x] 5.1 Обновить `bsl-agent/README.md`: как включить UI и как узнать URL через `bsl-agent ui url`.
- [x] 5.2 Добавить примеры для сценария с несколькими инстансами (выбор через `--roots` или `--instance-id`).
