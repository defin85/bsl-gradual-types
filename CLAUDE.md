# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## 🎯 О проекте

**BSL Gradual Type System** - современная система типов для языка 1С:Предприятие BSL, объединяющая статический анализ с runtime контрактами через градуальную типизацию.

### Почему новый репозиторий?
Это 5-я итерация системы типов. После 4 попыток в `bsl_type_safety_analyzer` решили начать с чистого листа, взяв лучшее из предыдущих подходов:
- Фасетная система из Unified Type System v2
- Градуальная типизация от Grock
- Эволюционная архитектура для постепенного развития

## 📊 Архитектура системы

### Слоистая архитектура

1. **Core Layer** (`src/core/`)
   - `types.rs` - центральная абстракция TypeResolution с уровнями уверенности (Known, Inferred, Unknown)
   - `resolution.rs` - pipeline разрешения типов с множественными источниками
   - `facets.rs` - система фасетов для представления типов в разных контекстах (Manager, Object, Reference, Metadata)
   - `contracts.rs` - runtime контракты для динамической проверки типов
   - `context.rs` - контекстное разрешение типов
   - `dependency_graph.rs` - граф зависимостей типов с обнаружением циклов
   - `type_checker.rs` - проверка и вывод типов
   - `standard_types.rs` - стандартные типы BSL

2. **Analysis Layer** 
   - **Parser** (`src/parser/`)
     - `lexer.rs` - токенизация BSL кода
     - `parser.rs` - синтаксический анализ на основе nom
     - `ast.rs` - Abstract Syntax Tree
     - `visitor.rs` - Visitor pattern для обхода AST
     - `graph_builder.rs` - построение графа зависимостей
   - **Query Parser** (`src/query/`)
     - `parser.rs` - парсер языка запросов 1С
     - `ast.rs` - AST для запросов
     - `batch.rs` - пакетные запросы с анализом зависимостей
     - `type_checker.rs` - проверка типов в запросах

3. **Adapter Layer** (`src/adapters/`)
   - `config_parser_xml.rs` - парсер Configuration.xml
   - `syntax_helper_parser.rs` - парсер синтакс-помощника и документации платформы
   - `platform_types_v2.rs` - резолвер платформенных типов с поддержкой синтакс-помощника
   - `facet_cache.rs` - кеширование фасетов

4. **Application Layer** (`src/bin/`)
   - `analyzer.rs` - CLI инструмент для анализа BSL файлов
   - `lsp_server.rs` - Language Server Protocol сервер
   - `build_index.rs` - утилита для построения индекса типов
   - `type_check.rs` - CLI для проверки типов

### Ключевые концепции

**TypeResolution** - не тип, а разрешение типа с уровнем уверенности:
```rust
pub struct TypeResolution {
    certainty: Certainty,        // Known | Inferred(0.0-1.0) | Unknown
    result: ResolutionResult,     // Concrete | Union | Conditional | Dynamic
    source: ResolutionSource,     // Static | Inferred | Runtime | Predicted
    metadata: ResolutionMetadata, // Debug info
}
```

**Фасетная система** - один тип может иметь разные представления:
- **Manager**: `Справочники.Контрагенты` - менеджер для работы со справочником
- **Object**: `СправочникОбъект.Контрагенты` - изменяемый объект
- **Reference**: `СправочникСсылка.Контрагенты` - ссылка на элемент
- **Metadata**: `Метаданные.Справочники.Контрагенты` - описание структуры

## 🔧 Команды для разработки

### Сборка и компиляция
```bash
# Сборка проекта в режиме разработки
cargo build

# Сборка релизной версии с оптимизациями
cargo build --release

# Быстрая сборка для разработки (оптимизация уровня 1)
cargo build --profile=dev-fast

# Проверка кода без компиляции
cargo check

# Форматирование кода
cargo fmt

# Линтинг кода
cargo clippy -- -D warnings
```

### Запуск исполняемых файлов
```bash
# Анализатор BSL файлов
cargo run --bin bsl-analyzer -- --file module.bsl --config /path/to/config

# LSP сервер для интеграции с IDE
cargo run --bin lsp-server

# Построение индекса типов из конфигурации
cargo run --bin build-index -- --config /path/to/config --platform-version 8.3.25

# Проверка типов
cargo run --bin type-check -- --file module.bsl
```

### Тестирование
```bash
# Запуск всех тестов
cargo test

# Только интеграционные тесты
cargo test --test "*"

# Конкретный тест
cargo test --test query_parser_test

# Запуск тестов с выводом print statements
cargo test -- --nocapture

# Проверка snapshot тестов (insta)
cargo insta test
cargo insta review
```

### Примеры использования и визуализация
```bash
# Основная визуализация системы типов
cargo run --example visualize_parser_v3         # Интерактивная визуализация с вкладками и фасетами

# Демонстрация парсера запросов
cargo run --example query_demo

# Демонстрации парсера синтакс-помощника
cargo run --example syntax_helper_unified_demo  # Унифицированный парсер
cargo run --example syntax_helper_parser_demo   # Демо основного парсера
cargo run --example syntax_helper_v3_demo       # Демо версии 3

# Тестирование категорий и типов
cargo run --example test_categories             # Тестирование категорий
cargo run --example test_completion             # Тестирование автодополнения

# Список всех примеров
cargo run --example
```

## 📚 Документация

Вся архитектурная документация находится в `docs/`:
- `docs/ARCHITECTURE.md` - главный архитектурный документ
- `docs/MIGRATION_PLAN.md` - план миграции кода из старого репозитория
- `docs/architecture/` - детальная архитектура
- `docs/design/` - дизайн-документы
- `docs/decisions/` - обоснование принятых решений

## 📊 Визуализация системы типов

Проект предоставляет интерактивную HTML визуализацию системы типов:

```bash
# Визуализация системы типов
cargo run --example visualize_parser_v3
```

Создаёт `type_hierarchy_v3_visualization.html` с:
- 📑 **Вкладками**: Типы, Индексы, Фасеты
- 🔍 **Поиском** по типам и категориям
- 📊 **Статистикой**: 4361 тип, 276 категорий, 6975 методов, 13357 свойств
- 🎯 **Системой фасетов** (Manager, Object, Reference)
- 🌐 **Двуязычными индексами** (русский/английский)

Подробнее см. [docs/implementation/visualization.md](docs/implementation/visualization.md)

## 🚀 Текущий статус разработки

**Текущая версия**: 0.4.0  
**Текущая фаза**: Phase 3.6 ✅ ЗАВЕРШЕНА / Phase 4 планируется  
**Дата последнего обновления**: Январь 2025

### Сводка прогресса:
- ✅ **Phase 1 (MVP)** - Базовая система типов
- ✅ **Phase 2** - Анализ кода и AST
- ✅ **Phase 3** - Поддержка языка запросов
- ✅ **Phase 3.5** - Парсер синтакс-помощника с интеграцией в LSP
- ✅ **Phase 3.6** - Оптимизация и улучшение визуализации
- 🔄 **Phase 4** - Расширенный анализ (следующая)

**Phase 1 (MVP)** - ✅ ЗАВЕРШЕНА:
- ✅ Базовые структуры данных и абстракции
- ✅ TypeResolution с уровнями уверенности  
- ✅ Полная фасетная система (Manager, Object, Reference, Constructor)
- ✅ Контракты для неопределённых типов
- ✅ Загрузка платформенных типов (хардкод с TODO)
- ✅ Парсинг Configuration.xml с табличными частями
- ✅ LSP сервер с hover и completion
- ✅ Автодополнение для Справочники.* и других типов

**Phase 2 (Анализ кода)** - ✅ ЗАВЕРШЕНА:
- ✅ Парсер BSL на основе nom
- ✅ Генерация AST (Abstract Syntax Tree)
- ✅ Граф зависимостей типов с обнаружением циклов
- ✅ Type checker с выводом типов
- ✅ Проверка совместимости типов
- ✅ Диагностики в LSP (ошибки, предупреждения)

**Phase 3 (Поддержка запросов)** - ✅ ЗАВЕРШЕНА:
- ✅ Парсер языка запросов 1С
- ✅ Поддержка составных имён таблиц (Документ.ПоступлениеТоваровУслуг)
- ✅ Временные таблицы (ПОМЕСТИТЬ/ИЗ ВТ_)
- ✅ Пакетные запросы с анализом зависимостей
- ✅ JOIN операции (ЛЕВОЕ/ПРАВОЕ/ПОЛНОЕ/ВНУТРЕННЕЕ СОЕДИНЕНИЕ)
- ✅ Агрегатные функции и GROUP BY
- ✅ Подзапросы и UNION
- ✅ Интеграция с системой типов

**Phase 3.5 (Парсер синтакс-помощника)** - ✅ ЗАВЕРШЕНА:
- ✅ Парсер ZIP архивов синтакс-помощника (rebuilt.shcntx_ru.zip, rebuilt.shlang_ru.zip)
- ✅ Извлечение глобальных функций и их сигнатур (476 функций)
- ✅ Парсинг ключевых слов языка с правильной категоризацией (22 ключевых слова)
- ✅ Извлечение глобальных объектов и типов данных (60 объектов)
- ✅ Парсинг системных перечислений (712 перечислений)
- ✅ Извлечение методов объектов (5617 методов)
- ✅ Извлечение свойств объектов (12044 свойства)
- ✅ Система TypeRef для нормализации типов (language:, context:, metadata_ref:)
- ✅ Определение фасетов из имён типов (CatalogManager → Manager)
- ✅ Интеграция с FacetRegistry для динамической регистрации фасетов
- ✅ Система кеширования фасетов (FacetCache с 30-дневной валидностью)
- ✅ Интеграция с PlatformTypesResolverV2
- ✅ Визуализация с правильной иерархией языковых конструкций
- ✅ Интеграция новых типов в LSP для автодополнения (476 функций, методы и свойства объектов)

**Результаты парсинга**:
- 476 глобальных функций с сигнатурами
- 60 глобальных объектов (Array, String, Map и др.)
- 712 системных перечислений с их значениями
- 5617 методов объектов с параметрами и возвращаемыми типами
- 12044 свойства объектов с типами
- 22 ключевых слова с категориями
- Правильная иерархия: "Общее описание встроенного языка" → подкатегории

**Phase 3.6 (Оптимизация парсера и визуализация)** - ✅ ЗАВЕРШЕНА:
- ✅ Многопоточный парсинг с rayon (улучшение производительности в 4-8 раз)
- ✅ Консолидация 4 версий парсера в единую унифицированную версию
- ✅ Корректная обработка категорий из catalog*.html файлов
- ✅ Извлечение человекочитаемых названий категорий (276 категорий)
- ✅ Автоматическая группировка типов по категориям
- ✅ Интерактивная HTML визуализация с иерархией категорий
- ✅ Поиск и фильтрация по категориям в визуализации
- ✅ Визуальное выделение топ-категорий по количеству типов

**Статистика системы типов**:
- 276 категорий с правильными названиями
- 4361 типов
- 6975 методов
- 13357 свойств

**Следующие приоритеты (Phase 4)**:
1. Flow-sensitive анализ типов
2. Межпроцедурный анализ
3. Type narrowing в условных конструкциях

## 🔄 Миграция из старого репозитория

Полезный код для переноса из `bsl_type_safety_analyzer`:
- **Приоритет 1**: XML парсер конфигурации (адаптировать под новую модель)
- **Приоритет 2**: Парсеры документации из `docs_integration/`
- **Приоритет 3**: Утилиты `fs_utils.rs` и `position.rs`
- **Позже**: VSCode extension, build tools

## ⚠️ Важные принципы разработки

1. **Честная неопределённость** - TypeResolution::Unknown лучше чем неправильный Inferred
2. **Эволюционность** - каждая фаза должна давать работающий функционал
3. **Модульность** - новые анализаторы добавляются через traits, не меняя ядро
4. **Градуальность** - начинаем со статики, добавляем динамику где нужно
5. **Контекст важен** - один тип может вести себя по-разному в разных контекстах (фасеты)

## 🛠️ Работа с зависимостями

Проект использует Rust с Cargo. Основные зависимости:
- `tower-lsp` и `lsp-types` для LSP функциональности
- `quick-xml` и `roxmltree` для парсинга XML конфигураций
- `tokio` для асинхронной работы в LSP сервере
- `tracing` для структурированного логирования
- `serde` для сериализации/десериализации
- `clap` для CLI аргументов
- `dashmap` для concurrent HashMap
- `lru` для кеширования
- `rayon` для параллельной обработки данных
- `indicatif` для визуализации прогресса

При добавлении новых зависимостей:
1. Используйте `cargo add <dependency>`
2. Проверяйте совместимость версий
3. Предпочитайте well-maintained crates

## 💡 Подсказки для разработки

### При добавлении новых типов
1. Начните с добавления в `types.rs`
2. Обновите резолвер в `resolution.rs`
3. Добавьте тесты

### При работе с фасетами
1. Проверьте контекст через `ContextResolver`
2. Активируйте правильный фасет
3. Возвращайте методы/свойства фасета

### При неопределённости типа
1. Попробуйте вывести из контекста
2. Если не удаётся - создайте контракт
3. Верните TypeResolution::Unknown с контрактом

## 🐛 Известные проблемы и технический долг

1. Union типы упрощены - возвращается первый тип вместо полного union
2. Межмодульный анализ не реализован
3. Flow-sensitive анализ в разработке
4. Один тест не проходит: test_parse_composite_type_attribute (не хватает атрибута "Сторона" в тестовых данных)

## 🏗️ Структура проекта

```
bsl-gradual-types/
├── src/
│   ├── core/           # Ядро системы типов
│   ├── parser/         # BSL парсер
│   ├── query/          # Парсер запросов 1С
│   ├── adapters/       # Адаптеры внешних данных
│   └── bin/            # Исполняемые файлы
├── tests/
│   ├── integration/    # Интеграционные тесты
│   └── fixtures/       # Тестовые данные
├── examples/           # Примеры использования
├── docs/              # Документация
└── CLAUDE.md          # Этот файл
```

## 📈 Метрики производительности

- [x] Загрузка конфигурации с 1000+ объектами: < 1 сек
- [x] Ответ LSP на автодополнение: < 100ms
- [x] Парсинг BSL файла 10000 строк: < 500ms
- [x] Анализ пакета из 10 запросов: < 50ms
- [x] Построение графа зависимостей: < 200ms для 100 функций
- [x] Runtime контракты генерируются для неизвестных типов
- [x] Парсер запросов поддерживает временные таблицы
- [x] Пакетные запросы с анализом зависимостей