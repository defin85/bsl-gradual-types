// Тестовый файл для проверки hover типов

Функция ТестоваяФункция()
    Перем Массив;
    Перем Строка;
    Перем Число;

    // Простое присваивание
    Строка = "Привет";
    Число = 42;

    // Создание массива
    Массив = Новый Массив;
    Массив.Добавить("Элемент");

    // Вызов метода
    Результат = Массив.Количество();

    Возврат Результат;
КонецФункции
