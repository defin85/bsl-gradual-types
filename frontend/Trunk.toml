[build]
target = "index.html"
dist = "../target/site"
no_sri = true

[watch]
watch = ["src"]

# ✅ Отключаем wasm-opt из trunk (используем build-wasm.js для оптимизации)
# Trunk не поддерживает отключение wasm-opt напрямую, поэтому используем --no-default-features
