Функция ТипЗначения(Значение)
    Если ТипЗнч(Значение) = Тип("Строка") Тогда
        Возврат "string";
    ИначеЕсли ТипЗнч(Значение) = Тип("Число") Тогда
        Возврат "number";
    Иначе
        Возврат "unknown";
    КонецЕсли;
КонецФункции
