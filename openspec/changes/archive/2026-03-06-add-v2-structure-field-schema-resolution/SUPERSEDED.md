# Superseded

Этот change superseded и заменен единым change:
- `add-v2-universal-collection-schema-resolution`

Причина: `Соответствие`, `Структура` и `ТаблицаЗначений` используют один snapshot-local pipeline schema/effect resolution и должны поддерживаться одним источником требований.
