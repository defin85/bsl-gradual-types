## 1. Контракт и спецификация
- [x] 1.1 Зафиксировать в delta-spec новые parity эндпоинты `/api/mcp/types|search|metrics` и их DTO.
- [x] 1.2 Зафиксировать правило выбора сессии: без `sessionId` работает только при ровно одной ready; иначе `INVALID_PARAMS`.
- [x] 1.3 Зафиксировать поведение SPA в MCP-режиме: те же вкладки, read-only, отдельное сообщение при 0/>1 ready.
- [x] 1.4 Зафиксировать single-session политику MCP: `workspace_open` идемпотентен при совпадении параметров и запрещает вторую сессию при отличающихся параметрах; для смены проекта требуется `workspace_close`.

## 2. Реализация: bsl-agent parity HTTP API
- [x] 2.1 Добавить роуты `/api/mcp/types`, `/api/mcp/search`, `/api/mcp/metrics` в HTTP UI `bsl-agent`.
- [x] 2.2 Реализовать общий helper выбора сессии (sessionId или ровно одна ready), общий для всех parity эндпоинтов.
- [x] 2.3 Переиспользовать существующую логику формирования `AnalysisResultDto` и `MetricsDto` без дублирования (предпочтительно).
- [x] 2.4 Добавить ограничение single-session в MCP tools: `workspace_open` идемпотентен при совпадении параметров и возвращает `INVALID_PARAMS` при попытке открыть вторую сессию с другими параметрами.

## 3. Реализация: frontend (единый SPA)
- [x] 3.1 В MCP-режиме переключить data-source для types/search/snapshot meta на `/api/mcp/*`.
- [x] 3.2 При 0/>1 ready сессии: показывать инструкцию и не делать parity вызовов без `sessionId`.
- [x] 3.3 Сохранить текущий MCP-дашборд (sessions/jobs) как “MCP особенности” в UI.

## 4. Тестирование и валидация
- [x] 4.1 Интеграционный тест: parity эндпоинты возвращают 200 при ровно одной ready сессии.
- [x] 4.2 Интеграционный тест: parity эндпоинты возвращают 400 при 0 ready (и защитно при >1 ready) без `sessionId`.
- [x] 4.3 Интеграционный тест: parity эндпоинты возвращают 400 при `sessionId` несуществующей/не-ready сессии.
- [x] 4.4 Интеграционный тест MCP: второй вызов `workspace_open` с теми же параметрами возвращает тот же `session_id` (идемпотентно).
- [x] 4.5 Интеграционный тест MCP: второй вызов `workspace_open` с другими параметрами отклоняется (`INVALID_PARAMS`).

## 5. Документация
- [x] 5.1 Обновить `docs/roadmap/mcp-bsl-agent/api.md`: добавить `/api/mcp/types|search|metrics` и правила выбора сессии.
- [x] 5.2 Обновить `bsl-agent/README.md`: описать ограничения parity UI (работает только при ровно одной ready сессии).
- [x] 5.3 Обновить документацию MCP: single-session ограничение и необходимость `workspace_close` для переключения проекта/roots.
