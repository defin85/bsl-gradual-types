// Тестовый файл для проверки type checker

// Работа с платформенными типами
Перем СписокКонтрагентов = Справочники.Контрагенты.Выбрать();
Перем НовыйКонтрагент = Справочники.Контрагенты.СоздатьЭлемент();

// Проверка вывода типов
Перем Число = 100;
Перем Строка = "Тест";
Перем Дата = '20241211';

// Функция с ошибкой типов
Функция ТестТипов(Параметр1, Параметр2 = 10)
    Перем Результат;
    
    // Несовместимая операция
    Результат = Параметр1 И Параметр2;  // Логическая операция с числом
    
    // Правильная операция
    Если Параметр1 > 0 Тогда
        Результат = Параметр1 + Параметр2;
    КонецЕсли;
    
    Возврат Результат;
КонецФункции

// Вызов функции с неправильным количеством аргументов
Перем Рез1 = ТестТипов(5, 10);  // Правильно
Перем Рез2 = ТестТипов(5);      // Правильно - есть значение по умолчанию
Перем Рез3 = ТестТипов();       // Ошибка - недостаточно аргументов

// Работа с массивами и структурами
Перем Массив = Новый Массив;
Массив.Добавить(1);
Массив.Добавить("строка");  // Массив смешанных типов

Перем Структура = Новый Структура("Ключ1, Ключ2", 100, "Значение");

// Проверка условных выражений
Если Число = "100" Тогда  // Предупреждение - сравнение числа со строкой
    Сообщить("Равно");
КонецЕсли;

// Цикл с ошибкой типа
Для Индекс = "1" По 10 Цикл  // Ошибка - строка вместо числа
    Сообщить(Индекс);
КонецЦикла;