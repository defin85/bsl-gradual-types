## 1. Requirements discovery
- [ ] Зафиксировать ожидания к форматированию BSL (минимум: отступы, переносы, пробелы вокруг операторов, оформление `Если/Тогда/КонецЕсли`, `Попытка/Исключение`, `Процедура/Функция`).
- [ ] Зафиксировать non-goals (например: не менять регистр идентификаторов; не перестраивать выражения).

## 2. Strategy evaluation
- [ ] Вариант A: собственный форматтер (AST‑based) в Rust.
- [ ] Вариант B: интеграция существующего форматтера/инструмента (если приемлемо по лицензии/качеству).
- [ ] Вариант C: не предоставлять форматирование (и явно рекомендовать внешний tool) — только если это осознанное решение.
- [ ] Выбрать стратегию и описать trade‑offs в `design.md`.

## 3. Next change plan
- [ ] Подготовить draft следующего change: `add-bsl-lsp-formatting` (implementation).

## 4. Validation
- [ ] `openspec validate evaluate-bsl-formatting --strict --no-interactive`
