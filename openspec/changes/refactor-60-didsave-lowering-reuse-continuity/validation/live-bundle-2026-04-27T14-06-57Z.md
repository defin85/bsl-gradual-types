# Live Bundle Validation - 2026-04-27T14:06:57Z

Bundle:
`/home/egor/code/temp/bsl-observability-incident-2026-04-27T14-06-57Z`

Runtime:

- Extension `0.4.160`.
- LSP server `0.4.160`, build `2026-04-27 16:35:42`, git `5bad5b72`.
- Diagnostics-save timeline contract `v22`.

## Result

The refactor-60 runtime path is validated by this bundle. The former v15 shape
does not recur: the second same-file save now uses a save-family seed and avoids
the previous seconds-scale unproved full rebuild.

| Trace | Version | First publish | Follow-up | Blocker bucket | Program lowering | Reuse | Source |
| --- | ---: | ---: | ---: | --- | ---: | --- | --- |
| `diagnostics-save-trace-1` | 11 | 99 ms | 1060 ms | `apply_lag` | 8 ms | `2088/0` | `borrowed` / `ast_cache_borrowed` |
| `diagnostics-save-trace-2` | 15 | 70 ms | 586 ms | `snapshot_with_deps` | 94 ms | `2042/46` | `save_family` / `save_family` |

Additional evidence:

- Both traces have `reuse_seed_candidate_count=1`.
- Neither trace reports `reuse_plan_failure_reason`.
- Neither trace reports a seed eviction reason.
- `observability_contract_violation total=0`.
- `invalid_saturation_metric` is absent.
- Completion ingress/egress remains bounded; completion traces are 0-227 ms.

## Projection Follow-up

The exported bundle initially reported one generated gap:

```text
Diagnostics save trace diagnostics-save-trace-2 is classified as snapshot_with_deps even though the ready-snapshot timeout leaf/checkpoint is program_lowering; exact program-lowering tail must be classified separately.
```

Raw fields show this was a false positive:

- `followup_ready_snapshot_timeout_leaf=null`
- `followup_ready_snapshot_parse_exec_core_build_exact_ready_snapshot_assembly_timeout_checkpoint=null`
- `followup_ready_snapshot_parse_exec_core_build_exact_ready_snapshot_assembly_program_lowering_ms=94`
- `followup_ready_snapshot_parse_exec_core_build_exact_ready_snapshot_assembly_program_lowering_reuse_plan_build_source=save_family`

The incident-bundle gap detector was narrowed so a dominant
`program_lowering` checkpoint only counts as tail evidence without an explicit
timeout leaf/checkpoint when it is seconds-scale (`>=1000ms`). Rebuilding this
bundle with the patched detector produces `gaps=[]`.

## Verification

- `npm --prefix vscode-extension run compile:fast`
- `BSL_TEST_GREP='program-lowering tail should keep missing reuse evidence fail-visible|fast program-lowering reuse should not be reported as a tail classification gap|program-lowering seed eviction evidence should survive report, raw JSON, and summary' node scripts/run-vscode-extension-tests.js`
- `npm --prefix vscode-extension run lint`
- Local rebuild of this bundle with patched `buildObservabilityIncidentBundle`
  produced `incidentReport.gaps=[]`.
